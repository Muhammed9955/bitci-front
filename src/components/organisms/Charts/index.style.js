import {css} from 'react-emotion'


export const fsCharts = css({
  marginBottom: 0,
  width: '100%',
  height: '100%',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'stretch',
})

export const nfsCharts = css`
  flex: 1;
  display: flex;
  flex-direction: column;
`

export const chartWrapper = css({
  flex: '1 1 270px',
  overflow: 'hidden',
})